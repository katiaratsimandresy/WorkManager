<core:FragmentDefinition xmlns="sap.m"
	xmlns:core="sap.ui.core" xmlns:l="sap.ui.layout" xmlns:f="sap.ui.layout.form">

	<Dialog title="{i18n>logout.title}"
		contentWidth="70%" showHeader="true" horizontalScrolling="false" draggable="true"
		class="sapUiPopupWithPadding">

		<content>
			<Text text="{i18n>logout.text}" />
		</content>
		<beginButton>
			<Button
				text="{i18n>logout.confirm}"
				press="confirmLogout" />
		</beginButton>
		<endButton>
			<Button
				text="{i18n>common.frag.button.cancel}"
				press="cancelLogout" />
		</endButton>
	</Dialog>
	
</core:FragmentDefinition>